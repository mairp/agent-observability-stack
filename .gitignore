.env
secrets/
*.secret
docker-compose.override.yml
data/
*/data/
onboard/registry.json
alertmanager/alertmanager.yml
media/frames/
*.bak*
# Host-specific live probe targets hold real internal IPs — keep local, never push.
# Sanitized *.example templates are committed instead (see bin/make-public.sh mapping).
prometheus/targets/blackbox-icmp.yml
prometheus/targets/blackbox-http.yml

# Local run state of the Ralph loop runner (events, transcripts, run ids) — never publish.
.ralph-runner/
# Host-local Prometheus scrape jobs (see prometheus/scrape.d/README.md).
prometheus/scrape.d/*.yml
# Python bytecode.
__pycache__/
*.pyc
